/**
 * Shared print-related constants used across Dashboard, QuickSign, and CustomPrint.
 *
 * Coding Standard #21: Static values extracted to configuration files.
 */

/** Default store identifier sent with every print request. */
export const DEFAULT_STORE_ID = "1234";

/** Default requestedBy user identifier. */
export const DEFAULT_REQUESTED_BY = "g197511";

/** Auto-dismiss duration (ms) for print success toasts. */
export const TOAST_AUTO_HIDE_MS = 5000;
